apiVersion: v1
kind: Service
metadata:
  name: argocd-ui-service
  namespace: argocd
spec:
  type: NodePort
  selector:
    app: argocd-ui
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
